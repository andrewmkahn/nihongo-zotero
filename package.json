{
  "name": "nihongo-zotero",
  "version": "0.1.0",
  "description": "Zotero 7 plugin for Japanese multilingual metadata",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "start": "node build.js && zotero -purgecaches -jsconsole"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "zotero-types": "^1.3.22"
  }
}
